services:
  - type: web
    name: fis-tarama-sistemi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_VISION_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
